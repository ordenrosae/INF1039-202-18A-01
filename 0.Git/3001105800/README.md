sdk
```
def binary_search(list, item):

low=0

high=len(list)-1

while below

mid = (low + high)

guess= list [mid]

if guess = = item returm mid

if guess > item:

high = mid-1

else

low = mid+1

return one

my_list= (1,3, 5, 7, 9)
```
